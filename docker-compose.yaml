version: "3"

services:
  # 前端服务
  ve-blog-element:
    build:
      context: ./
      dockerfile: Dockerfile
    image: ve-blog-element:latest  # 设置新的镜像名称和标签
    container_name: ve-blog-element
    restart: always
    ports:
      - "8888:80"
